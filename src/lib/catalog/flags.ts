export const catalogFlags = {
  /** Public listings hide coming-soon / out-of-stock unless a collection would be empty. */
  hideUnavailableProducts: true,
  lowDepthMaxCm: 30,
  narrowWidthMaxCm: 80,
} as const;
